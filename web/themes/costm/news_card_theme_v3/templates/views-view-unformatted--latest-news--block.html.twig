{# Optional: if you prefer Twig grid instead of CSS-only #}
<div class="row g-4">
  {% for row in rows %}
    <div class="col-12 col-md-6 col-lg-4">
      {{ row.content }}
    </div>
  {% endfor %}
</div>